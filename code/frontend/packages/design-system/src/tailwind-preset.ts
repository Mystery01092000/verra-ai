import { tokens } from './tokens';
export const verraPreset = {
  theme: { extend: { colors: {
    indigo: tokens.color.accent, 'indigo-600': tokens.color.accent600,
    ink: tokens.color.ink, cream: tokens.color.cream,
  } } },
};
