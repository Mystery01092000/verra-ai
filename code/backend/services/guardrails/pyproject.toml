[project]
name = "verra-guardrails"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = ["fastapi>=0.111", "uvicorn[standard]>=0.30", "pydantic>=2.7", "httpx>=0.27", "verra-shared"]
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
