# verra-holdings
Consolidated holdings store + deterministic net-worth/allocation engine
(`verra_shared.holdings.consolidate` — no LLM; advisory flags carry guideline citations).
Run: `uvicorn app.main:app --reload --port 8083`
