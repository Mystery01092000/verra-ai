# Copy to .env.local (never commit real secrets — guarded by .claude/hooks/guard-bash.sh)
NODE_ENV=development
PORT=8080
DATABASE_URL=postgres://verra:verra@localhost:5432/verra
REDIS_URL=redis://localhost:6379
OBJECT_STORE_ENDPOINT=http://localhost:9000
# --- AI providers (zero-retention contracts; loaded via secrets manager in prod) ---
ANTHROPIC_API_KEY=
SECONDARY_PROVIDER_API_KEY=
# --- Auth ---
OIDC_ISSUER=
OIDC_CLIENT_ID=
# --- Observability ---
OTEL_EXPORTER_OTLP_ENDPOINT=
