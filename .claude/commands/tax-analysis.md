---
description: Run a Verra-style tax analysis for a client (ingest → analyze → scenarios → cited plan).
argument-hint: [client name or document]
---
Use the `verra-tax-analysis` skill. For **$1**: ingest documents, compute the current position with
deterministic calculators, surface opportunities with citations, model ≥2 scenarios side by side, then
prepare a client-ready letter + report for human approval. Never present a figure without a source.
