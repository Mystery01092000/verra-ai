---
description: Build a new Verra UI screen/component in Next.js (code/frontend) using design tokens.
argument-hint: [screen or component name]
---
Use `verra-frontend` + `verra-design-system`. Build **$1** in code/frontend with `@verra/design-system`
(tokens only), the prototype's patterns, streaming where relevant, and approval-gate + citation affordances.
