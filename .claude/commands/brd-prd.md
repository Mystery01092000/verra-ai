---
description: Create or update a Verra requirements document (BRD / PRD / spec).
argument-hint: [BRD|PRD|spec topic]
---
Use the `verra-requirements-writing` skill. Produce/update **$1** with consistent structure, unique
requirement IDs, MoSCoW priorities, Given/When/Then acceptance criteria, and cited figures
(`docs/research-sources.md`). Output a `.docx` consistent with the existing documents.
